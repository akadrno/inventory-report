{
  "name": "ppac-inventory-api",
  "version": "1.0.0",
  "description": "Azure Functions backend for PPAC Inventory — RBAC + elevated data proxy",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start"
  },
  "dependencies": {
    "@azure/data-tables": "^13.3.0",
    "@azure/functions": "^4.5.0",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "rimraf": "^5.0.5",
    "typescript": "^5.6.0"
  }
}
